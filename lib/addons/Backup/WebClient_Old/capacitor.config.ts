import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.als.v.dua.nol.workalogi',
  appName: 'als-app',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
