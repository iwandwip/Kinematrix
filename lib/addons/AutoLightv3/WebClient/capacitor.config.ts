import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.als.v.tiga.nol.workalogi',
  appName: 'AutoLight-V3-WebClient',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;